# Lab

Optional images for `/lab` entries — not required, each entry works with just text. Drop in as `<slug>.jpg` matching the entry's filename in `src/content/lab/` on the Kozalak repo (e.g. `beatdemo-compositor.jpg`) and it can be wired in.
